version 18.0
set more off

/*******************************************************************************
    Purpose:
        Optional machine-specific path overrides for paths that should not be
        hardcoded in tracked do-files.

    Usage:
        Copy this file to config_local_paths.do and edit locally.
        Keep config_local_paths.do untracked.
*******************************************************************************/

* Example overrides:
* global dsf "D:/restricted_inputs/dsf"
* global EXTRA_RAWDIR "D:/project_inputs"
