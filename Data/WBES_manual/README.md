# WBES Manual Intake

Use this folder for manually added World Bank Enterprise Survey raw inputs that are not yet part of a scripted download or import workflow.

For each country or intake batch:

- keep the original raw file unchanged
- record the source, date obtained, and any manual paste or export step
- create reproducible cleaning code downstream rather than editing the raw file

Do not overwrite raw manual inputs.

