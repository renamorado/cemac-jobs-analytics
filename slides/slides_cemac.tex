\documentclass[aspectratio=169]{beamer}

\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{comment}
\usepackage{graphicx}
\usepackage{xcolor}

\IfFileExists{lato.sty}{
    \usepackage[default]{lato}
}{}

\definecolor{pgpblue}{RGB}{0,114,178}
\definecolor{pgpred}{RGB}{213,94,0}
\definecolor{pgpgreen}{RGB}{0,158,115}
\definecolor{pgpbackground}{RGB}{255,255,255}

\setbeamercolor{background canvas}{bg=pgpbackground}
\setbeamercolor{normal text}{fg=black,bg=pgpbackground}
\setbeamercolor{structure}{fg=pgpblue}
\setbeamercolor{frametitle}{fg=pgpblue,bg=pgpbackground}
\setbeamercolor{title}{fg=black}
\setbeamercolor{itemize item}{fg=pgpblue}
\setbeamercolor{itemize subitem}{fg=pgpblue}
\setbeamercolor{enumerate item}{fg=pgpblue}

\setbeamerfont{title}{series=\bfseries,size=\Large}
\setbeamerfont{frametitle}{series=\bfseries,size=\large}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{footline}[frame number]
\setbeamertemplate{itemize items}[circle]
\setbeamertemplate{section in toc}[sections numbered]

\newcommand{\figsource}[1]{%
    \par\vspace{0.04cm}
    {\centering\tiny #1\par}
}
\newcommand{\censusfigsource}{\figsource{Source: INS Cameroon, RGE3 (2023-2024); calculations by authors.}}
\newcommand{\bdffigsource}{\figsource{Source: Cameroon administrative tax/BDF panel; calculations by authors.}}
\newcommand{\censusbdffigsource}{\figsource{Sources: INS Cameroon, RGE3 (2023-2024), and Cameroon administrative tax/BDF panel; calculations by authors.}}
\newcommand{\wbesfigsource}{\figsource{Source: World Bank Enterprise Surveys, latest available CEMAC waves; calculations by authors.}}
\newcommand{\cmrwbesfigsource}{\figsource{Sources: World Bank Enterprise Surveys, Cameroon 2024, and Cameroon administrative tax/BDF panel; calculations by authors.}}

\title{CEMAC Jobs Analytics}
\subtitle{Cameroon elasticities and WBES trade diagnostics}
\author{}
\date{\today}

\begin{document}

\begin{frame}[plain]
    \titlepage
\end{frame}

\begin{frame}{Roadmap}
    \tableofcontents
\end{frame}

\section{BDF Cleaning Results}

\begin{frame}{Data cleaning overview}
    \begin{itemize}
        \item The working panel is organized at the firm-year level, so duplicate handling changes both sample size and identification.
        \item Economic sector is based on the NACAM variable, which according to documentation its based on a Regional harmonized classifier linked to ISIC.
        \item The cleaning note isolates the two steps that most affect downstream interpretation: duplicate resolution and the NACAM-to-ISIC crosswalk.
    \end{itemize}
\end{frame}

\begin{frame}{Firm-Year Duplicate Cleaning}
    \footnotesize
    \begin{itemize}
        \item Working key: \texttt{firmid} $\times$ \texttt{fin\_yr}
        \item Perfect duplicates would be collapsed with original row order as the tie-breaker.
        \item Conflicting duplicate firm-years are fully removed from the cleaned analysis file, while we figure out the best approach for handling them.
    \end{itemize}

    \vspace{0.2cm}
    \centering
    \resizebox{0.76\textwidth}{!}{
        \begin{tabular}{lr}
            \input{../output/tables/cmr_bdf_cleaning_note_duplicate_summary.tex}
        \end{tabular}
    }
\end{frame}
\begin{comment} 
    % Lets confirm this but I think that we are not using the ISIC codes for the elasticity analysis, so we can skip this slide if we want to save time.
    % We can also move it to the appendix if we want to keep it for reference but not spend time on it in the main presentation.
\begin{frame}{NACAM-to-ISIC Crosswalk}
    \small
    \begin{itemize}
        \item Observed legacy NACAM codes are audited directly from the Cameroon BDF file.
        \item Legacy branch labels are recovered from the INS RGE nomenclature document, while the mapping to \texttt{NACAM rev.1} and ISIC comes from the official \texttt{NACAM rev.1} passage tables.
        \item ISIC Rev.4 divisions are assigned only when the official mapping is one-to-one.
        \item Ambiguous branches remain flagged for manual review rather than being forced into a noisy division code.
        \item Since the NACAM is already based on the ISIC structure, the analysis file retains the original NACAM variable for elasticity estimation and reporting, while the ISIC variable is used for sectoral aggregation and cross-country comparisons.
    \end{itemize}

    \vspace{0.4cm}
    \centering
    \resizebox{0.75\textwidth}{!}{
        \begin{tabular}{lr}
            \input{../output/tables/cmr_bdf_cleaning_note_crosswalk_summary.tex}
        \end{tabular}
    }
\end{frame}
\end{comment}

\begin{frame}{Cleaning Takeaways}
    \begin{itemize}
        \item The cleaned panel resizes the sample from 9,325 to 9,068 observations after removing duplicate firm-years.
        \item There are 127 conflicting firm-years, accounting for 257 dropped obs. Those observations are excluded rather than resolved ad hoc.
        %\item The crosswalk covers 37 observed legacy NACAM codes, but only 17 map cleanly to a single ISIC Rev.4 division. 
        \item To avoid introducing unwanted noise we keep the analysis using NACAM classification and link it to ISIC aggregates when possible.
        %\item The remaining 20 codes are explicitly flagged for manual review, which keeps sector harmonization transparent before pooled industry comparisons.
    \end{itemize}
\end{frame}

\section{Cameroun Census data on firm counts, employment, and revenue}

\begin{frame}{Cameroun Census Context}
    \begin{itemize}
        \item The 2024 RGE census gives a whole-economy snapshot of establishments in Cameroon.
        \item Census activity labels are harmonized to the same NACAM sectors used in the administrative elasticity analysis.
        \item The next slides use headquarter-level data to show sector scale in firm counts, log employment, log annual revenue, and log revenue per worker.
        \item After this economy-wide view, the administrative tax panel focuses on formal firms observed over time, using the administrative data.
    \end{itemize}
\end{frame}

\begin{frame}{Census Firm Counts by Harmonized NACAM Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_census_firm_count_by_nacam.png}
    \censusfigsource
\end{frame}

\begin{frame}{Census Log Employment by Harmonized NACAM Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_census_total_employment_by_nacam.png}
    \censusfigsource
\end{frame}

\begin{frame}{Census Log Revenue by Harmonized NACAM Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_census_total_revenue_by_nacam.png}
    \censusfigsource
\end{frame}

\begin{frame}{Census Log Revenue per Worker by Harmonized NACAM Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_census_turnover_per_worker_by_nacam.png}
    \censusfigsource
\end{frame}

\section{Cameroun Sectoral Elasticities}

\begin{frame}{Specification and Sample}
    \footnotesize
    \begin{equation}
        \ln(E_{it}) = \alpha_i + \delta_{st} + \sum_s \beta_s \left( \ln(X_{it}) \times D_{is} \right) + \varepsilon_{it}
    \end{equation}

    \begin{itemize}
        \item The unit of observation is a firm-year $(i,t)$.
        \item $E_{it}$ is employment for firm $i$ in year $t$.
        \item $X_{it}$ is either value added or total revenue, estimated in separate regressions.
        \item $D_{is}$ equals 1 if firm $i$ belongs to NACAM sector $s$, and 0 otherwise.
        \item Sector is treated as a firm characteristic, so each firm contributes to one NACAM interaction term.
        \item $\alpha_i$ are firm fixed effects; $\delta_{st}$ are NACAM-sector-by-fiscal-year fixed effects.
        \item $\beta_s$ is the elasticity for firms in sector $s$, identified through the interaction between $\ln(X_{it})$ and the sector indicator $D_{is}$.
        \item No additional firm-level controls are included beyond these fixed effects.
        \item Standard errors are clustered at the firm level; reported sectors have at least 30 observations and 10 firms in both outcome samples.
    \end{itemize}
\end{frame}

\begin{frame}{Employment Distribution by Fiscal Year}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_nacam_results_en_labels_ln_emp_density_by_year.png}
    \bdffigsource
\end{frame}

\begin{frame}{Value-Added Elasticity Specification}
    \small
    \begin{equation}
        \ln(E_{it}) = \alpha_i + \delta_{st} + \sum_s \beta^{VA}_s \left( \ln(VA_{it}) \times D_{is} \right) + \varepsilon_{it}
    \end{equation}

    \begin{itemize}
        \item Estimates use the Cameroon administrative firm-year panel.
        \item Firm fixed effects absorb time-invariant firm differences at the taxpayer/firm level.
        \item NACAM-sector-by-fiscal-year fixed effects absorb annual shocks common to firms in the same detailed activity.
        \item No age, ownership, exporter-status, or other firm controls are included.
        \item The plotted point for each sector is $\beta^{VA}_s$ with a firm-clustered 95 percent interval.
    \end{itemize}
\end{frame}

\begin{frame}{Value-Added Elasticities Across NACAM Sectors}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_nacam_results_en_labels_va_coefficients.png}
    \bdffigsource
\end{frame}

\begin{frame}{Total-Revenue Elasticity Specification}
    \small
    \begin{equation}
        \ln(E_{it}) = \alpha_i + \delta_{st} + \sum_s \beta^{R}_s \left( \ln(R_{it}) \times D_{is} \right) + \varepsilon_{it}
    \end{equation}

    \begin{itemize}
        \item $R_{it}$ is total revenue, estimated in a separate regression from value added.
        \item Fixed effects are at the firm level and NACAM-sector-by-fiscal-year level.
        \item No additional firm-level controls are included beyond these fixed effects.
        \item The plotted point for each sector is $\beta^{R}_s$ with standard errors clustered at the firm level.
    \end{itemize}
\end{frame}

\begin{frame}{Total-Revenue Elasticities Across NACAM Sectors}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_nacam_results_en_labels_tot_rev_coefficients.png}
    \bdffigsource
\end{frame}

\begin{frame}{Cross-Model Comparison}
    \begin{itemize}
        \item The next plot compares the sector-specific value-added and total-revenue elasticities.
        \item Each point is one NACAM sector retained by the common inclusion rule.
        \item Points near the 45-degree line indicate sectors where both revenue proxies give similar employment responses.
        \item Large gaps flag sectors where value added and gross revenue tell different stories.
    \end{itemize}
\end{frame}

\begin{frame}{Cross-Model Consistency}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_nacam_results_en_labels_scatter.png}
    \bdffigsource
\end{frame}

\begin{frame}{Fixed-Effect Robustness Check}
    \begin{itemize}
        \item FE means fixed effects. All robustness specifications keep taxpayer/firm fixed effects and firm-clustered standard errors.
        \item Baseline: firm FE plus detailed NACAM-sector-by-fiscal-year FE.
        \item Looser alternative: firm FE plus fiscal-year FE only.
        \item Intermediate alternative: firm FE plus broad-activity-group-by-fiscal-year FE.
        \item No age, ownership, exporter-status, or other firm-level controls are added in these robustness checks.
    \end{itemize}
\end{frame}

\begin{frame}{Value-Added Elasticity Robustness}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_nacam_fe_robustness_va_coefficients.png}
    \bdffigsource
\end{frame}

\begin{frame}{Total-Revenue Elasticity Robustness}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_nacam_fe_robustness_tot_rev_coefficients.png}
    \bdffigsource
\end{frame}

\begin{frame}{Census Turnover-Employment Specification}
    \footnotesize
    \begin{equation}
        \ln(E_i) = \alpha_s + \sum_s \beta_s
        \left[\ln(R_i) \times D_{is}\right] + \varepsilon_i
    \end{equation}

    \begin{itemize}
        \item The unit of observation is a Census headquarters firm $i$.
        \item $E_i$ is employment and $R_i$ is annual turnover from RGE3.
        \item $D_{is}$ equals 1 if firm $i$ belongs to NACAM sector $s$.
        \item $\alpha_s$ are detailed NACAM sector intercepts.
        \item $\beta_s$ is the cross-sectional employment elasticity with respect to annual turnover in sector $s$.
        \item There are no firm fixed effects, year fixed effects, or NACAM-year fixed effects because the Census has no panel time variation.
        \item Standard errors are heteroskedasticity-robust; reported sectors have at least 30 positive-employment and positive-turnover headquarters firms.
    \end{itemize}
\end{frame}

\begin{frame}{Census Turnover-Employment Elasticities}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_census_turnover_employment_elasticity_coefficients.png}
    \censusfigsource
\end{frame}

\begin{frame}{Census Turnover-Employment Robustness}
    \footnotesize
    \begin{itemize}
        \item Census estimates are cross-sectional slopes; tax/BDF estimates are panel total-revenue elasticities with firm and NACAM-year fixed effects.
        \item The solid black line marks equality between the two estimates; the dashed grey line shows the fitted relationship across overlapping NACAM sectors.
    \end{itemize}
    \vspace{-0.12cm}
    \centering
    \includegraphics[width=\textwidth,height=0.56\textheight,keepaspectratio]{../output/figures/cmr_census_vs_bdf_turnover_employment_elasticity.png}
    \censusbdffigsource
\end{frame}

\begin{frame}{Duplicate Robustness Check}
    \begin{itemize}
        \item The baseline specification excludes conflicting duplicate firm-years from the cleaned panel.
        \item The robustness specification keeps those duplicate rows as separate observations.
        \item Firm fixed effects and clustered standard errors still use the original firm identifier, so duplicate rows do not become new firms.
        \item The next plot compares baseline and duplicate-inclusive elasticities sector by sector.
    \end{itemize}
\end{frame}

\begin{frame}{Baseline vs Duplicate-Inclusive Elasticities}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_nacam_results_duplicate_robustness_comparison.png}
    \bdffigsource
\end{frame}

\begin{frame}{Elasticity, Productivity, and Sector Scale}
    \begin{itemize}
        \item The opportunity maps compare sector employment elasticities with log sector value added per worker.
        \item The solid horizontal line separates employment-expanding and employment-contracting sectors; the dashed vertical line marks median productivity.
        \item Bubble size identifies the lower, middle, or upper tercile of each scale measure across the 29 plotted sectors; legends report the numerical ranges.
        \item Labels identify the two highest and two lowest elasticities in each panel plus the highest-productivity sector.
    \end{itemize}
\end{frame}

\begin{frame}{Opportunity Map: Total Sector Revenue}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{../output/figures/cmr_elasticities_va_worker_bubble_total_revenue.png}
    \bdffigsource
\end{frame}

\begin{frame}{Opportunity Map: Total Sector Employment}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{../output/figures/cmr_elasticities_va_worker_bubble_total_employment.png}
    \bdffigsource
\end{frame}

\begin{frame}{Opportunity Map: Average Firm Revenue}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{../output/figures/cmr_elasticities_va_worker_bubble_avg_revenue.png}
    \bdffigsource
\end{frame}

\begin{frame}{Opportunity Map: Average Firm Employment}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{../output/figures/cmr_elasticities_va_worker_bubble_avg_employment.png}
    \bdffigsource
\end{frame}

\begin{frame}{Administrative Tax/BDF Asset Diagnostics}
    \begin{itemize}
        \item The RGE/Census file does not include the dictionary-listed asset field, so asset diagnostics use the administrative tax/BDF panel instead.
        \item Net fixed assets are the preferred capital proxy because they most closely represent firm capital stock in the current BDF variables.
        \item Each firm contributes once to the sector totals, using its latest available positive asset observation.
        \item Net total assets are shown as a broader balance-sheet comparison.
    \end{itemize}
\end{frame}

\begin{frame}{BDF Net Fixed Assets by Harmonized NACAM Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_bdf_net_fixed_assets_by_nacam.png}
    \bdffigsource
\end{frame}

\begin{frame}{BDF Net Total Assets by Harmonized NACAM Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.70\textheight,keepaspectratio]{../output/figures/cmr_bdf_net_total_assets_by_nacam.png}
    \bdffigsource
\end{frame}

\begin{frame}{Revenue-Elasticity Sector Deciles}
    \vspace{-0.35cm}
    \centering
    \tiny
    \resizebox{\textwidth}{0.38\textheight}{
        \begin{tabular}{lrrrrr}
            \input{../output/tables/cmr_nacam_results_en_labels_tot_rev_decile_ranking.tex}
        \end{tabular}
    }
    \vspace{0.03cm}

    {\scriptsize Decile 10 is highest; decile 1 is lowest.}
\end{frame}

\begin{frame}{Main Takeaways}
    \begin{itemize}
        \item Elasticities vary meaningfully across sectors rather than lining up around a single Cameroon-wide coefficient.
        %\item Several sectors show positive employment responses under both value added and total revenue, with NACAM 17 standing out most clearly.
        \item Negative or weak value-added elasticities remain concentrated in a small set of sectors, which is useful for prioritizing follow-up diagnostics.
        %\item The cleaning and harmonization steps are not bookkeeping only; they determine which firm-years and sector codes enter the elasticity analysis.
    \end{itemize}
\end{frame}

\section{B-READY Mapped WBES Constraints}

\begin{frame}[shrink=10]{B-READY/WBES Constraint Extension}
    \begin{itemize}
        \item The analysis uses the 16 questions marked \texttt{Priority = yes} in the reviewed B-READY workbook.
        \item Cameroon WBES firms are mapped from four-digit ISIC Rev.4 main-product codes to reviewed NACAM sectors using the official INS bridge.
        \item The horizontal axis shows WBES-weighted sector constraints; the vertical axis shows administrative total-revenue employment elasticities.
        \item Bubble area shows average annual total sector employment using fixed groups across indicators, not WBES precision.
        \item Orange sectors exceed the published Cameroon constraint benchmark and have an employment elasticity of at least 0.20.
        \item Labels identify opportunity sectors and the two largest-employment sectors with valid indicator responses.
        \item Results are descriptive, not causal; sectors without indicator responses are omitted from the corresponding chart.
        \item Fourteen indicators reproduce the national B-READY value within two points. The selected market-concentration item is unavailable in Cameroon microdata, and the VAT-refund denominator requires further review.
    \end{itemize}
\end{frame}

\begin{frame}{Access to Land Constraints by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_reg12_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Courts Constraints by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_disp6_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Discouraged Loan Applicants by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_fin26_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Loan Decision Time by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_fin27_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Cost to Receive E-Payments by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_fin30_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Access to Finance Constraints by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_fin33_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Export Border-Compliance Time by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_tr18_u_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Export Compliance Cost by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_tr20_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Import Border-Compliance Time by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_tr24_u_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Import Compliance Cost by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_tr25_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Customs and Trade Constraints by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_tr26_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Transport Constraints by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_in23_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Labor-Regulation Constraints by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_wk28_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\begin{frame}{Electrical Outages by Sector}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/bready_wbes_cmr_in2_high_elasticity.png}
    \cmrwbesfigsource
\end{frame}

\section{CEMAC WBES Trade Analysis}

\begin{frame}{Phase 3: WBES Trade Diagnostics}
    \begin{itemize}
        \item Phase 3 uses the latest available World Bank Enterprise Survey wave for CEMAC countries.
        \item The first step is a cleaning and availability audit for sales, employment, sector, export, import, and input-origin variables.
        \item Gabon is not retained in the main descriptive panels because its latest available WBES wave is 2009 and has no ISIC Rev.4 coverage.
        \item The WBES formal-sector sampling frame covers non-agricultural private firms, so sector rows use eligible activity aggregates rather than agriculture.
    \end{itemize}
\end{frame}

\begin{frame}[plain]
    \centering
    {\small\bfseries WBES Descriptive Statistics}

    \vspace{0.03cm}
    \centering
    \tiny
    \resizebox{0.96\textwidth}{0.46\textheight}{
        \begin{tabular}{lccccccc}
            \input{../output/tables/cemac_wbes_trade_descriptive_stats_deck.tex}
        \end{tabular}
    }

    \vspace{0.03cm}
    {\tiny Sales use fiscal-year official exchange rates. Brackets report standard errors.}
\end{frame}

\begin{frame}{WBES Country Coverage}
    \footnotesize
    \centering
    \resizebox{0.88\textwidth}{!}{
        \begin{tabular}{lrrrrr}
            \input{../output/tables/cemac_wbes_trade_country_coverage.tex}
        \end{tabular}
    }

    \vspace{0.25cm}
    {\scriptsize Retained excludes Gabon from the main descriptive panels because its latest available wave is 2009 and lacks ISIC Rev.4.}
\end{frame}

\begin{frame}{Core Variable Availability}
    \vspace{-0.2cm}
    \centering
    \tiny
    \resizebox{\textwidth}{!}{
        \begin{tabular}{lrrrrrrrrrrrrrr}
            \input{../output/tables/cemac_wbes_trade_variable_availability.tex}
        \end{tabular}
    }

    \vspace{0.2cm}
    {\scriptsize Counts are unweighted establishments in retained CEMAC latest-wave surveys.}
\end{frame}

\begin{frame}{Negative Special-Code Audit}
    \vspace{-0.2cm}
    \centering
    \tiny
    \resizebox{\textwidth}{!}{
        \begin{tabular}{lrrrrrrrrrrr}
            \input{../output/tables/cemac_wbes_trade_negative_values.tex}
        \end{tabular}
    }

    \vspace{0.2cm}
    {\scriptsize Negative WBES special codes are audited before being recoded to missing for nonnegative levels, percentages, and binary responses.}
\end{frame}

\begin{frame}{Winsorization Cutoffs}
    \footnotesize
    \centering
    \resizebox{0.88\textwidth}{!}{
        \begin{tabular}{lrrrr}
            \input{../output/tables/cemac_wbes_trade_winsor_cutoffs.tex}
        \end{tabular}
    }

    \vspace{0.25cm}
    {\scriptsize Sales and input costs are winsorized at the 5th and 95th percentiles within each country wave.}
\end{frame}

\begin{frame}{Exporter Participation}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/cemac_wbes_trade_exporter_share.png}
    \wbesfigsource
\end{frame}

\begin{frame}{Export Intensity Among Exporters}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/cemac_wbes_trade_export_intensity.png}
    \wbesfigsource
\end{frame}

\begin{frame}{Import and Foreign-Input Participation}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/cemac_wbes_trade_import_participation.png}
    \wbesfigsource
\end{frame}

\begin{frame}{Two-Way Trader Status}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/cemac_wbes_trade_two_way_trader.png}
    \wbesfigsource
\end{frame}

\begin{frame}{Focal WBES Revenue-Exporter Specification}
    \small
    \begin{equation}
        \ln(E_i) = \alpha_c + \beta_c \ln(R_i) + \gamma_c Exporter_i
        + \delta_c\left[\ln(R_i) \times Exporter_i\right]
        + \theta_c Z_i + \lambda_{c q(i)} + \varepsilon_i,
        \quad i \in c
    \end{equation}

    \begin{itemize}
        \item $c$ indexes the country. The model is estimated separately within each country.
        \item WBES is cross-sectional, so there are no firm fixed effects or panel controls.
        \item $Z_i$ controls for log firm age, foreign ownership share, and government/state ownership share.
        \item $\lambda_{c q(i)}$ are country-specific ISIC Rev.4 section fixed effects.
        \item Estimates use WBES weights and robust standard errors, not clustered standard errors.
        \item $\beta_c$ is the non-exporter slope; $\beta_c + \delta_c$ is the exporter slope.
        \item The focal estimate is $\delta_c$, the exporter minus non-exporter slope difference.
        \item Display rule: at least 30 firms overall and at least 10 firms in each exporter group.
    \end{itemize}
\end{frame}

\begin{frame}{Country Revenue-Exporter Interaction Estimates}
    \IfFileExists{../output/tables/cemac_wbes_revenue_exporter_interaction_country.tex}{
    \vspace{-0.3cm}
    \centering
    \tiny
    \resizebox{\textwidth}{!}{
        \begin{tabular}{lrrrrrrrrrr}
            \input{../output/tables/cemac_wbes_revenue_exporter_interaction_country.tex}
        \end{tabular}
    }

    \vspace{0.1cm}
    {\scriptsize The difference column is the focal exporter minus non-exporter slope estimate.}
    }{
    \centering
    \vfill
    \large Results pending regeneration of the WBES interaction stage.

    \vspace{0.4cm}
    \normalsize Required input: \texttt{Data/Analysis/wbes\_trade\_clean.dta}
    \vfill
    }
\end{frame}

\begin{frame}{Country Employment Responses to Revenue by Exporter Status}
    \centering
    \IfFileExists{../output/figures/cemac_wbes_revenue_exporter_interaction_country.png}{
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/cemac_wbes_revenue_exporter_interaction_country.png}
    \wbesfigsource
    }{
    \vfill
    \large Figure pending regeneration of the WBES interaction stage.
    \vfill
    }
\end{frame}

\begin{frame}{Cameroon WBES Activity-Group Specification}
    \footnotesize
    \begin{equation}
        \ln(E_i) = \alpha_a + \beta_a \ln(R_i) + \gamma_a Exporter_i
        + \delta_a\left[\ln(R_i) \times Exporter_i\right]
        + \theta_a Z_i + \lambda_{a q(i)} + \varepsilon_i,
        \quad i \in a
    \end{equation}

    \begin{itemize}
        \item $a$ indexes broad Cameroon WBES activity groups; the model is estimated separately within each group.
        \item Groups combine ISIC Rev.4 sections: manufacturing; construction/utilities; trade/hospitality/transport; and other services.
        \item WBES is cross-sectional, so there are no firm fixed effects.
        \item $Z_i$ controls for log firm age, foreign ownership share, and government/state ownership share.
        \item $\lambda_{a q(i)}$ are activity-specific ISIC Rev.4 section fixed effects where there is within-activity variation.
        \item Estimates use WBES weights and robust standard errors, not clustered standard errors.
        \item Only two groups satisfy the display rule. The excluded groups are construction/utilities with 8 exporters and other services with 9, below the required 10 exporters.
    \end{itemize}
\end{frame}

\begin{frame}{Cameroon Activity Employment Responses to Revenue (Diagnostic)}
    \centering
    \IfFileExists{../output/figures/cemac_wbes_revenue_exporter_interaction_cameroon_activity.png}{
    \includegraphics[width=\textwidth,height=0.68\textheight,keepaspectratio]{../output/figures/cemac_wbes_revenue_exporter_interaction_cameroon_activity.png}
    \wbesfigsource

    {\scriptsize Activity estimates are exploratory diagnostics; suppressed groups remain documented in the generated sample audit.}
    }{
    \vfill
    \large Diagnostic figure pending regeneration of the WBES interaction stage.
    \vfill
    }
\end{frame}

\begin{frame}{Supporting WBES Export-Value Specification}
    \small
    \begin{equation}
        \ln(E_i) = \alpha + \beta \ln(X_i^+) + \gamma Exporter_i
        + \theta Z_i + \lambda_{q(i)} + \varepsilon_i
    \end{equation}

    \begin{itemize}
        \item These supporting models use export value rather than total revenue.
        \item Latest-wave WBES estimates remain weighted cross-sectional associations.
        \item $X_i^+$ is export value, with log export value set to zero for non-exporters in the all-firm model.
        \item The exporter dummy separates the extensive margin from the positive-export-value slope.
        \item $Z_i$ controls for log firm age, foreign ownership share, and government/state ownership share.
        \item $\lambda_{q(i)}$ are ISIC Rev.4 section fixed effects.
        \item Rows with fewer than 30 observations are suppressed; benchmark rows are equal-country averages.
    \end{itemize}
\end{frame}

\begin{frame}{WBES Country Trade-Employment Estimates}
    \vspace{-0.2cm}
    \centering
    \tiny
    \resizebox{\textwidth}{!}{
        \begin{tabular}{lrrrrrrrr}
            \input{../output/tables/cemac_wbes_trade_elasticity_country.tex}
        \end{tabular}
    }

    \vspace{0.15cm}
    {\scriptsize Slopes are weighted cross-sectional log employment/log export-value associations.}
\end{frame}

\begin{frame}{WBES Country Specification: All Firms}
    \small
    \begin{equation}
        \ln(E_i) = \alpha_c + \beta_c \ln(X_i^+) + \gamma_c Exporter_i
        + \theta_c Z_i + \lambda_{c q(i)} + \varepsilon_i
    \end{equation}

    \begin{itemize}
        \item Estimated separately by country using WBES weights.
        \item Non-exporters stay in the sample with $\ln(X_i^+) = 0$.
        \item $Z_i$ controls for log firm age, foreign ownership share, and government/state ownership share.
        \item $\lambda_{c q(i)}$ are country-specific ISIC Rev.4 section fixed effects.
        \item The plotted coefficient is $\beta_c$; it is descriptive, not causal.
    \end{itemize}
\end{frame}

\begin{frame}{WBES Country Associations: All Firms}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/cemac_wbes_trade_elasticity_country_all_firms.png}
    \wbesfigsource
\end{frame}

\begin{frame}{WBES Country Specification: Exporters Only}
    \small
    \begin{equation}
        \ln(E_i) = \alpha_c + \beta_c \ln(X_i) + \theta_c Z_i
        + \lambda_{c q(i)} + \varepsilon_i,\quad X_i > 0
    \end{equation}

    \begin{itemize}
        \item Estimated separately by country among firms with positive export value.
        \item This is the intensive-margin association within exporters.
        \item $Z_i$ controls for log firm age, foreign ownership share, and government/state ownership share.
        \item $\lambda_{c q(i)}$ are country-specific ISIC Rev.4 section fixed effects.
        \item Countries with fewer than 30 exporters are suppressed in the plot.
    \end{itemize}
\end{frame}

\begin{frame}{WBES Country Associations: Exporters Only}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/cemac_wbes_trade_elasticity_country_exporters_only.png}
    \wbesfigsource
\end{frame}

\begin{frame}{Cameroon WBES Activity Estimates}
    \vspace{-0.2cm}
    \centering
    \tiny
    \resizebox{0.96\textwidth}{!}{
        \begin{tabular}{lrrrrrrr}
            \input{../output/tables/cemac_wbes_trade_elasticity_cameroon_activity.tex}
        \end{tabular}
    }

    \vspace{0.15cm}
    {\scriptsize Activity groups use WBES-ISIC aggregates}
\end{frame}

\begin{frame}{Cameroon Activity Specification: All Firms}
    \small
    \begin{equation}
        \ln(E_i) = \alpha_a + \beta_a \ln(X_i^+) + \gamma_a Exporter_i
        + \theta_a Z_i + \lambda_{a q(i)} + \varepsilon_i
    \end{equation}

    \begin{itemize}
        \item Estimated within Cameroon WBES activity groups.
        \item This is a diagnostic adaptation of the elasticity idea to cross-sectional survey data.
        \item $Z_i$ controls for log firm age, foreign ownership share, and government/state ownership share.
        \item $\lambda_{a q(i)}$ are activity-specific ISIC Rev.4 section fixed effects where there is within-activity variation.
        \item Activity rows pass the all-firm sample threshold, but exporter-only rows may be suppressed.
    \end{itemize}
\end{frame}

\begin{frame}{Cameroon Activity Associations: All Firms}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/cemac_wbes_trade_elasticity_cameroon_activity_all_firms.png}
    \wbesfigsource
\end{frame}

\begin{frame}{Cameroon Activity Specification: Exporters Only}
    \small
    \begin{equation}
        \ln(E_i) = \alpha_a + \beta_a \ln(X_i) + \theta_a Z_i
        + \lambda_{a q(i)} + \varepsilon_i,\quad X_i > 0
    \end{equation}

    \begin{itemize}
        \item Estimated among Cameroon exporters within WBES activity groups.
        \item $Z_i$ controls for log firm age, foreign ownership share, and government/state ownership share.
        \item $\lambda_{a q(i)}$ are activity-specific ISIC Rev.4 section fixed effects where there is within-activity variation.
        \item Rows are shown only when at least 30 exporter observations are available.
        \item These results are best read as sample diagnostics for follow-up, not sector multipliers.
    \end{itemize}
\end{frame}

\begin{frame}{Cameroon Activity Associations: Exporters Only}
    \centering
    \includegraphics[width=\textwidth,height=0.72\textheight,keepaspectratio]{../output/figures/cemac_wbes_trade_elasticity_cameroon_activity_exporters_only.png}
    \wbesfigsource
\end{frame}

\end{document}
